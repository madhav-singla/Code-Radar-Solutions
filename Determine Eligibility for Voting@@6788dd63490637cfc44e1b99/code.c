#include <stdio.h>

int main() {
    int a;
    scanf("%d ", &a);

    // Check if it's a valid triangle
    if (a>=18) {
printf(Eligible);
    } else {
    printf("Not Eligible")
    }

    return 0;
}
